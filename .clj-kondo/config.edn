;; byte-array is a real cljs.core function (typed-array backed), but
;; clj-kondo's built-in cljs.core stub is missing it -- same known gap
;; class as boolean-array. False positive, not a real portability bug.
{:linters {:unresolved-symbol {:exclude [byte-array]}}}
